name: Buffer Fix Catchup 118 [RETIRED]

# Historical one-off recovery workflow for closed issue #118.
# Retained as audit history, but deliberately non-executable so it cannot
# mutate an old Buffer post or create permanent Actions failure noise.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired one-off Buffer catch-up for closed issue #118."
