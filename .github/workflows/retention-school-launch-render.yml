name: Retention School launch render retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed
        run: |
          echo 'This workflow is retired. Retention School creatives must use the exact owner-approved asset; do not regenerate or substitute.'
          exit 1
