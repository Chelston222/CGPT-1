import {errorResponse,id,json,normalisePhone,now,parseBody,readList,requireAdmin,writeList} from './_shared.mjs';
export default async event=>{try{requireAdmin(event);const prospects=await readList('prospects');if(event.httpMethod==='GET')return json(200,{ok:true,prospects});if(event.httpMethod!=='POST')return json(405,{ok:false,error:'METHOD_NOT_ALLOWED'});const b=parseBody(event);if(!b.business||!b.phone||!b.gap)throw new Error('MISSING_FIELDS');const record={id:id(),business:String(b.business).trim(),contact:String(b.contact||'').trim(),phone:normalisePhone(b.phone),sector:String(b.sector||'').trim(),website:String(b.website||'').trim(),gap:String(b.gap).trim(),permission:b.permission||'unknown',offer:b.offer||'Send personalised gap summary',status:'review',createdAt:now(),updatedAt:now()};prospects.unshift(record);await writeList('prospects',prospects);return json(201,{ok:true,prospect:record})}catch(e){return errorResponse(e)}};
