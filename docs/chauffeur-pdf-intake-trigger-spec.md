# Chauffeur PDF intake trigger spec

Owner-only trigger title:

`[IMAP PDF INTAKE] rs-li-chauffeur-open-quote-recovery-001`

The workflow accepts no payload from the issue body. All expected values are locked in the workflow itself and are independently re-verified before any queue or media commit.
