# Chauffeur PDF intake transport

Transport source is a Gmail-delivered attachment in `hello@222emails.com`, read by GitHub Actions through the existing PrivateEmail IMAP credential. No public temporary URL is required.
